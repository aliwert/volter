# Guide

Long-form mdBook guide (`mdbook init` here once there's enough of an API to
document). Keep this separate from generated `cargo doc` API reference —
this is for narrative, task-oriented content: "build your first API",
"add authentication", "testing your handlers", etc.
